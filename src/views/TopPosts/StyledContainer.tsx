import styled from 'styled-components';

const StyledContainer = styled.div`
    .loader svg {
        width: 150px;
        height: 150px;
        margin: 0 auto;
    }
`;

export default StyledContainer;